spring:
  application:
    name: knowledgemesh

  datasource:
    url: jdbc:postgresql://localhost:5432/knowledge_mesh
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true

server:
  port: 8080